#!/bin/sh
set -eu

if ! command -v uv >/dev/null 2>&1; then
    printf '%s\n' 'K2 Region Lab requires uv: https://docs.astral.sh/uv/getting-started/installation/' >&2
    exit 127
fi

exec uv run --extra web k2lab-runpod "$@"
